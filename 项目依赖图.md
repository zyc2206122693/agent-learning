# 个人理财 Agent：项目依赖图

本文中的箭头表示“调用方依赖被调用方”。

## 文件依赖总览

```mermaid
flowchart TD
    subgraph UI["应用与入口层"]
        FS["fund_server.py<br/>网页、HTTP API、聊天接口"]
        IA["investment_assistant.py<br/>单 Agent 入口"]
        MA["multi_agent.py<br/>多 Agent 编排"]
        UFD["update_fund_dashboard.py<br/>生成静态看板"]
        DC["demo_classify.py"]
        DCON["demo_concurrency.py"]
    end

    subgraph Agent["Agent 编排层"]
        LC["llm_client.py<br/>模型客户端、run_tool_loop、MockLLM"]
        AT["agent_tools.py<br/>Tool、ToolRegistry"]
        NC["news_classifier.py<br/>新闻结构化分类"]
    end

    subgraph Domain["业务计算层"]
        PA["portfolio_analysis.py<br/>风险、集中度、调仓、流水"]
        ND["news_data.py<br/>新闻抓取、缓存、过滤"]
        FD["fund_data.py<br/>本地数据读写、原子保存"]
    end

    subgraph Data["本地数据"]
        FUNDS["funds.json<br/>真实持仓"]
        SETTINGS["portfolio_settings.json<br/>目标比例与阈值"]
        TX["transactions.json<br/>交易流水"]
        HISTORY["fund_history.json<br/>历史组合数据"]
        CACHE["fund_cache.json / news_cache.json"]
    end

    subgraph External["外部服务"]
        LLM["DeepSeek Anthropic API"]
        SINA["新浪基金净值"]
        EAST["东方财富新闻"]
        BROWSER["浏览器 / localStorage"]
    end

    FS --> IA
    FS --> PA
    FS --> ND
    FS --> FD
    FS --> BROWSER

    IA --> AT
    IA --> LC
    IA --> FD
    IA --> PA
    IA --> ND

    MA --> IA
    MA --> LC
    MA --> AT

    NC --> AT
    NC --> LC
    NC -.运行时调用.-> ND
    ND -.可选 LLM 分类.-> NC

    PA --> FD
    UFD --> FD
    UFD --> SINA

    LC --> LLM
    ND --> EAST

    FD --> FUNDS
    FD --> HISTORY
    FD --> CACHE
    PA --> SETTINGS
    PA --> TX

    DC --> LC
    DC --> NC
    DCON --> LC
    DCON --> AT
```

## 聊天调用链路

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as fund_server.py
    participant I as investment_assistant.py
    participant L as llm_client.py
    participant T as ToolRegistry
    participant P as portfolio_analysis.py
    participant D as JSON数据

    U->>F: 我的组合风险怎么样？
    F->>I: ask(question, history)
    I->>L: run_tool_loop(messages, tools)
    L->>L: 模型选择 analyze_portfolio
    L->>T: execute("analyze_portfolio")
    T->>P: analyze_portfolio()
    P->>D: 读取持仓与风险配置
    D-->>P: 真实数据
    P-->>T: 结构化分析结果
    T-->>L: tool_result
    L->>L: 模型基于结果生成解释
    L-->>F: final_text + tool_calls
    F-->>U: NDJSON 渐进式回答
```

## 文件职责

| 文件 | 直接本地依赖 | 定位 |
|---|---|---|
| `agent_tools.py` | 无 | 最底层工具抽象 |
| `llm_client.py` | 无 | 模型调用、工具循环和 MockLLM |
| `fund_data.py` | JSON 文件 | 数据访问和原子保存 |
| `portfolio_analysis.py` | `fund_data.py` | 确定性组合计算 |
| `news_data.py` | `news_classifier.py` | 新闻抓取、缓存和过滤 |
| `news_classifier.py` | `agent_tools.py`、`llm_client.py` | LLM 结构化分类 |
| `investment_assistant.py` | 工具、模型、数据、分析 | 单 Agent 业务入口 |
| `multi_agent.py` | 单 Agent、模型、工具 | 路由、专家和收敛 |
| `fund_server.py` | Agent、分析、数据、新闻 | 前端与 HTTP API |
| `update_fund_dashboard.py` | `fund_data.py` | 静态看板生成 |
| `evaluate.py` | `multi_agent.py` | Agent 路由评测 |
| `test_portfolio_analysis.py` | `portfolio_analysis.py` | 业务计算单元测试 |

## 推荐阅读顺序

```text
agent_tools.py
    ↓
llm_client.py
    ↓
fund_data.py + portfolio_analysis.py
    ↓
investment_assistant.py
    ↓
multi_agent.py
    ↓
fund_server.py
```

先理解工具抽象和 Agent 循环，再看确定性业务工具如何注册给模型，最后阅读 HTTP API 和前端。

## 当前架构注意点

`news_data.py` 和 `news_classifier.py` 存在运行时双向依赖。目前使用延迟导入避免模块初始化失败，但长期更适合抽出独立的新闻数据类型，或者让分类器不再反向依赖抓取层，从架构上消除循环依赖。

聊天接口目前使用 NDJSON 做应用层渐进式传输：工具调用和模型回答完成后，后端再分块发送文本。它不是模型原生逐 Token SSE。
