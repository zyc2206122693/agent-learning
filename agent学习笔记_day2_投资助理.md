# Agent 学习 · 第 2 天笔记：投资助理（真数据 + 编排循环）

> 记录日期：2026-08-17
> 承接：第 1 课（工具注册表 + LLM 结构化分类）。本课把"假工具"换成"真数据"，让模型**自主编排**。

---

## 一、本课目标（从"结构"到"自主"）

第 1 课用 `tool_choice: tool` **强制**模型走固定输出（结构化抽取）。
第 2 课用 `tool_choice: auto` **让模型自己决定**要查哪些工具、再回答——这就是 Agent 的"自主决策"。

**对比一句话（面试 tool_choice 完整答案）**：
- `tool` 强制 → 结构化输出/分类契约
- `auto` 自主编排 → 真正的 Agent 决策

---

## 二、新增文件：`investment_assistant.py`

**4 个真数据工具**（读 `funds.json` + `news_data`）：

| 工具 | 作用 | 入参 |
|---|---|---|
| `get_portfolio_summary` | 组合总览：总市值/累计投入/收益率/方向分布 | 无 |
| `get_fund_detail` | 单只基金详情（市值/投入/收益/定投状态） | `code` |
| `list_funds_by_theme` | 按方向模块查持仓（纳指100/A股科技/稳健固收/观察停投） | `theme` enum |
| `get_news` | 真新闻，按关联方向过滤 | `related` enum + `limit` |

**编排核心**：
```python
client.run_tool_loop(registry, system=_SYSTEM_PROMPT,
    messages=[{"role":"user","content": question}],
    tool_choice={"type":"auto"}, max_tokens=800)
```

**真模型实测**（问："040046 值多少？纳指100最近有什么新闻？"）：
- 模型自主调了 `get_fund_detail(040046)` + `get_news(纳指)`
- 用真实数据组织回答（市值 102.49、收益率 +28.11% + 2 条纳指新闻 + "该关注什么"分析）
- 3 轮收敛（第1轮 2 个 tool_use → 回填 → 第2轮 text）

---

## 三、顺手改进：`MockLLM`（第 1 课共享代码）

离线 mock 之前有两个问题，已修：
1. **收到 tool_result 后改为返回 text** → 模拟"调一次工具后作答"，离线能干净收敛，不再无限空转。
2. **按工具 input_schema 生成合法参数**（enum 取首项/字符串填占位）→ 离线能真实执行工具，不会因参数类型错误被兜底。

**面试点**：MockLLM 和 LLMClient 实现同一套接口（多态），让整条链路离线/在线都能跑、且可确定性测试。

---

## 四、本课的核心面试题（承接第 1 课 Q8）

**Q8. "查持仓→查新闻→再分析"拆一个 Agent 还是多个 Agent？**
- **答**：先问是**线性同上下文**还是**需要不同专业视角**。查几个数据源 → 一个 Agent 调多个工具（共享上下文、编排简单）。角色差异明显（研究员/顾问/总结）才拆多 Agent，上游产出**结构化中间结果**传下游，设收敛出口。**先单 Agent，真正需要再拆**。

**Q9. 并行 vs 串行，何时不能并行？**
- 独立查询可并行（查基金 + 查新闻）；工具 B 参数要 A 的结果就必须分阶段/串行。

---

## 五、路线图

- **第 3 步**：意图路由（把问题先分到"查净值/查新闻/查持仓"，再交给对应子 Agent）
- **第 4 步**：多 Agent + 评估
- **可选加固**：`run_tool_loop` 从串行升级为真并发（`ThreadPoolExecutor` + 每工具独立超时 + 失败降级）

---

## 六、遗留重点（复习提醒）

- **Q4 并发耗时**：并发=取最大值（30s），串行=累加（31.1s）；慢工具是硬瓶颈 → 攻击它（超时/降级/缓存/做快）。
- **Q2**：`tool_result` 不需要符合 `input_schema`（那是入参格式）；要用 `tool_use_id` 对上号回填。

---

*个人 Agent 学习笔记，非投资建议。*
