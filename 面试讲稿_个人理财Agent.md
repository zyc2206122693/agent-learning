# 个人理财 Agent：面试讲稿

## 一句话介绍

这是一个连接真实持仓、交易流水、基金净值和财经新闻的个人理财 Agent。它能够回答持仓收益、组合风险、集中度和调仓模拟等问题，并通过工具调用保证数据和计算可追溯。

## 3 分钟项目介绍

我从零实现了一个小型但完整的 Agent 系统，而不只是调用一次大模型 API。

系统分为五层：

1. **数据层**：用 JSON 保存持仓、交易流水和目标比例，并从外部数据源更新基金净值与新闻。
2. **确定性计算层**：组合占比、集中度、收益和调仓金额由 Python 计算，不让大模型直接做金融计算。
3. **工具层**：统一封装工具名称、自然语言描述、JSON Schema 和执行器。
4. **Agent 编排层**：模型选择工具，程序执行并回填 `tool_result`，循环直到模型生成最终答案。
5. **应用层**：通过本地 HTTP API 提供组合看板和上下文聊天界面。

项目同时实现了 MockLLM、最大执行步数、工具超时、异常回填、输入校验和行为评测，保证 Agent 不只是能演示，也具备基本的工程可控性。

## 核心 Agent 循环

```text
用户问题
  ↓
模型读取消息和工具定义
  ↓
模型返回 tool_use
  ↓
程序执行真实 Python 工具
  ↓
使用 tool_use_id 回填 tool_result
  ↓
再次调用模型，直到得到最终文本
```

伪代码：

```python
for step in range(max_steps):
    response = llm.complete(messages=messages, tools=tools)
    tool_calls = find_tool_calls(response)

    if not tool_calls:
        return final_text(response)

    results = execute_tools(tool_calls)
    messages.append(response)
    messages.append(build_tool_results(results))
```

关键点是：工具结果必须使用原来的 `tool_use_id` 回填，否则模型无法判断结果属于哪次调用。循环还必须配置最大步数、超时和异常处理，避免无限调用。

## 为什么金融计算不交给 LLM

LLM 适合做意图理解、工具规划和自然语言解释，但不适合充当事实数据库或精确计算引擎。

例如调仓金额由代码计算：

```python
target_value = total_value * target_weight
difference = target_value - current_value
```

系统采用：

```text
LLM 理解意图
→ 工具读取真实数据
→ Python 确定性计算
→ LLM 解释结果
```

这样可以减少求和错误、遗漏持仓、比例不等于 100% 和数字前后矛盾等幻觉。

## Tool 抽象

```python
@dataclass
class Tool:
    name: str
    description: str
    input_schema: dict
    executor: Callable
```

工具描述是模型决策空间的一部分。描述过于模糊或职责重叠会导致误调用，所以每个工具尽量保持单一职责，并明确适用场景和输入约束。

主要工具包括：

- `get_portfolio_summary`：读取组合总览；
- `get_fund_detail`：查询单只基金；
- `analyze_portfolio`：计算风险和集中度；
- `simulate_rebalance`：模拟目标比例调仓；
- `get_transaction_summary`：汇总交易流水；
- `get_news`：查询财经新闻。

## 单 Agent 与多 Agent

生产主路径使用单 Agent，因为当前工具数量有限，单 Agent 成本更低、延迟更小、上下文更完整。

项目也实现了协调器与专家模式：

```text
holdings → 持仓专家
planning → 组合规划专家
news     → 新闻专家
```

多 Agent 适合工具域明显分离、权限不同或任务可以并行的场景。当前多 Agent 更多用于展示意图路由、专家工具隔离和结果收敛能力，而不是为了多 Agent 而多 Agent。

## 上下文和记忆

浏览器最多保存最近 20 条聊天记录，每次仅向模型发送最近 8 条有效消息。

这属于短期会话记忆，可以支持“那调仓呢”一类追问，同时限制上下文成本和历史噪声。

如果扩展长期记忆，我会把用户偏好、风险承受能力、投资目标和历史决策结构化存储，再按当前问题检索，而不是把全部聊天记录塞进 Prompt。

## 渐进式传输

聊天接口通过 NDJSON 发送事件：

```json
{"type":"meta","tools":["analyze_portfolio"]}
{"type":"delta","text":"你的组合"}
{"type":"delta","text":"存在集中度风险"}
{"type":"done"}
```

当前实现是应用层渐进式传输：Agent 完成工具和模型调用后，后端分段发送答案。它改善了显示体验，但不是模型原生逐 Token SSE。

真正的 Token Streaming 需要处理模型 SSE、跨事件累积工具参数、执行工具后重新开启下一轮流，以及将最终文本 Token 实时转发给浏览器。

## 测试和评测

测试分两层：

1. **确定性单元测试**：验证集中度、风险占比、调仓金额守恒、非法目标比例和交易汇总。
2. **Agent 行为评测**：验证用户问题是否路由到正确意图和工具域。

Agent 评测不能只看最终答案像不像，还应检查：

- 工具选择是否正确；
- 参数是否合法；
- 是否调用了不该调用的工具；
- 调用次数是否合理；
- 工具事实是否被最终回答正确引用。

## 工程安全

当前已实现：

- 服务仅监听 `127.0.0.1`；
- 静态资源白名单；
- 请求体大小限制；
- 数值和基金代码校验；
- 目标比例必须合计 100%；
- Markdown 先转义再渲染；
- 聊天默认只读，不自动交易；
- 工具超时和最大执行步数；
- 真实配置、持仓、流水和缓存不进入 Git。

## 当前不足与下一步

- API Key 仍兼容从本地 JSON 读取，生产环境应改成环境变量或密钥管理服务；
- 当前 JSON 适合单用户，扩展后应迁移到 SQLite/PostgreSQL 并增加事务；
- Markdown 渲染器是轻量自研版本，可以替换为成熟安全库；
- 还没有模型原生 Token Streaming；
- 新闻内容需要更严格的提示注入隔离；
- 评测集需要增加工具参数、事实一致性、安全拒答和长对话用例。

## 高频追问

### 为什么不用 LangChain？

我先手写编排循环，是为了真正理解消息协议、`tool_use/tool_result` 关联、终止条件和异常处理。当前规模下裸实现依赖少、行为透明；如果流程扩展到需要持久化状态、断点恢复和复杂图结构，我会考虑 LangGraph。

### 为什么使用 JSON？

这是个人单用户原型，JSON 足够轻量。写入采用临时文件和原子替换降低损坏风险。如果扩展到多用户和并发写入，则迁移到数据库并引入事务和数据迁移。

### 如何降低模型误调用？

通过单一职责工具、清晰描述、JSON Schema、运行时校验、专家工具隔离和离线评测共同约束。涉及写操作时，还应增加权限分级和用户二次确认。

### 工具失败怎么办？

将异常转换成结构化 `tool_result` 返回模型，让模型决定解释、重试或切换工具，同时设置超时、最大重试和最大执行步数，避免整个 Agent 崩溃或无限循环。

## 1 分钟总结

这个项目的价值不只是理财业务，而是完整实现了一个小型 Agent 系统：模型负责规划，工具负责真实数据和确定性计算，编排循环负责执行与收敛，评测系统验证中间行为，前端提供上下文聊天和工具可见性。同时通过 Mock、超时、最大步数、输入校验和只读权限提高可控性。
